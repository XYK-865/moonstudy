# MoonStudy 参赛项目说明

## 项目目标

MoonStudy 是一个使用 MoonBit 编写的本地优先学习打卡与统计助手。它解决的实际问题是：初学者希望记录每天学了什么、投入了多少时间，却不想注册账号、配置服务器或维护复杂表格。用户只需填写日期、学习内容、分钟数、完成情况和备注，应用就会自动保存并计算统计。

## 核心交付

应用支持新增、历史查看、删除、今日/本周/累计时长、完成率、主题统计、最近 7 天柱状图、过去一年完成量格子图、Markdown 日报和周报、亮/暗/跟随系统主题，以及完整 JSON 备份与恢复。所有学习记录默认保存在当前浏览器 localStorage 中，无账号、无遥测、无上传接口。

## MoonBit 的作用

MoonBit 不是项目标签，而是业务核心：记录校验、存储封套解析、增删操作、概览统计、主题聚合、按日聚合、日报、周报和备份恢复校验都由 MoonBit 完成，并编译成 JavaScript ESM。浏览器 JavaScript 只负责 DOM、localStorage 搬运、下载和绘制，不重新计算非零业务数据。

## 工程质量

项目使用本地 Git 保存开发历史；保留架构母版、需求确认、变更日志和 Codex/Zcode 换手记录。当前包含 24 项 MoonBit 单元与契约测试，并以桌面版 Microsoft Edge 为目标浏览器。损坏数据不会被当作空数据覆盖，恢复文件在写入前必须经过 MoonBit 校验。构建、测试和演示步骤都可以从公开仓库复现。

## 边界与后续

当前版本专注单人、本地、无需登录的学习记录，不做账号、云同步、服务器数据库或多人协作。后续可在不破坏现有数据契约的前提下增加编辑、计时器和更丰富的学习计划。
