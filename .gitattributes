# GitHub 语言统计标记（用户 2026-09-23 确认的 v004 方案第 3 项）
#
# 1. MoonBit 编译产物：由 `moon build --target js` 生成，不是手写源码。
#    标记为生成文件后不再计入 GitHub 语言统计，避免 314KB 产物被算成 JavaScript。
#    构建产物改名或新增时，必须在这里登记新名称。
public/moonstudy-core_*.mjs linguist-generated=true
public/moonstudy-app_*.mjs linguist-generated=true

# 2. moon.mod 被 linguist 误判为 AMPL（同为 .mod 扩展名）；它是 MoonBit 模块文件，
#    归入 MoonBit 而不是 AMPL。
moon.mod linguist-language=MoonBit
