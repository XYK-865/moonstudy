# MoonStudy 验收演示说明

## 准备

1. 安装 Python 3 和桌面版 Microsoft Edge。
2. 在仓库根目录进入 `public`，运行 `python -m http.server 8124 --bind 127.0.0.1`。
3. 在 Edge 打开 `http://127.0.0.1:8124/`。

## 五分钟演示

1. 新增一条已完成的“MoonBit 基础”40 分钟记录，观察今日、本周、累计、完成率和主题统计同步更新。
2. 再新增一条未完成记录，确认完成率变化；刷新页面，确认两条记录仍在。
3. 查看最近 7 天柱状图和过去一年格子图；格子颜色只按当天已完成记录数量变化。
4. 选择“日报”，生成并下载 `.md`；切换“周报”再次生成和下载。
5. 在界面颜色中依次选择亮色、暗色、跟随系统，并刷新确认偏好保留。
6. 点击“导出完整备份”得到 JSON；删除一条记录，再通过“选择备份恢复”恢复，确认记录和统计回到导出时状态。
7. 输入接近 100 字的学习内容，确认“投入最多”卡片保持两行，悬停仍可查看全文。

## 异常演示

- 选择损坏的 JSON 文件恢复，页面应拒绝导入，现有记录不变。
- localStorage 中存在损坏内容时，页面应停止新增和报告生成，并允许下载原始内容，不得静默覆盖。

## 开发者复现

在仓库根目录依次运行：

```text
moon fmt
moon check --target js
moon test --target js
moon build --target js
node --check public/host.js
```

预期：检查 0 错误、0 警告，24 项测试全部通过，JS 构建成功。
