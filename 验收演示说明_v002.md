# MoonStudy v004 验收演示说明

## 准备

1. 安装 Python 3 和桌面版 Microsoft Edge。
2. 在仓库根目录进入 `public`，运行 `python -m http.server 8124 --bind 127.0.0.1`。
3. 在 Edge 打开 `http://127.0.0.1:8124/`。

## 五分钟演示

1. 新增一条已完成的“MoonBit 基础”40 分钟记录，观察今日、本周、累计、完成率和主题统计同步更新。
2. 再新增一条未完成记录，确认完成率变化；刷新页面，确认两条记录仍在。
3. 查看最近 7 天柱状图和过去一年格子图；格子颜色只按当天已完成记录数量变化。
4. 查看“与上周同进度对比”；当上周没有记录时显示“无对比基线”，有记录时显示分钟、条数、完成率和投入最多主题。
5. 选择“日报”生成并下载 `.md`；切换“周报”，选择本周或历史周内任意日期后生成和下载。
6. 在界面颜色中依次选择亮色、暗色、跟随系统，并刷新确认偏好保留。
7. 点击“导出完整备份”得到 JSON；删除一条记录，再通过“选择备份恢复”恢复，确认记录和统计回到导出时状态。
8. 输入接近 100 字的学习内容，确认“投入最多”卡片保持两行，悬停仍可查看全文。

## 异常演示

- 选择损坏的 JSON 文件恢复，页面应拒绝导入，现有记录不变。
- localStorage 中存在损坏内容时，页面应停止新增和报告生成，并允许下载原始内容，不得静默覆盖。
- 超过 5 MB 的备份文件应停止读取；用户取消恢复确认时，当前数据不能变化。

## 开发者复现

在仓库根目录依次运行：

```text
moon fmt
moon check --target js
moon test --target js
moon build --target js --release
node --check ../03_测试与验证/浏览器首版验收_v004.mjs
```

预期：项目代码检查 0 错误、0 警告（Rabbita 间接依赖存在单独的上游弃用提示），31 项 MoonBit 测试全部通过，release 构建成功；隔离桌面 Edge 中 15 类验收全部通过。

## MoonBit 主体性检查

- 页面只加载 `moonstudy-app_v004.mjs`；`public/host.js` 已移除。
- 表单、统计、图表、主题、报告、记录、备份、恢复和损坏数据保护均由 MoonBit/Rabbita 状态树控制。
- 文件读取、下载、localStorage 和确认框只通过 `app/browser.mbt` 中的薄 FFI 调用浏览器能力。
- `public/index.html` 中仅保留 Rabbita 在 Edge 隐藏页运行所需的调度兼容代码，不含 UI 或业务计算。
